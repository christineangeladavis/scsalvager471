# Tauri desktop app source — built locally / via GitHub Actions, not on Vercel.
# Leading slash anchors to repo root so api/desktop/* (the serverless
# functions that drive the Settings → Desktop App section) DOES get
# deployed.
/desktop/

# Build artifacts that don't belong in the Vercel deployment.
node_modules/
dist/
.claude/
