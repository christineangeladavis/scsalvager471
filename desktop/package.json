{
  "name": "scsalvager-desktop",
  "version": "0.2.8",
  "private": true,
  "description": "SCSalvager Desktop — Tauri shell wrapping scsalvager.net with native tray, hotkeys, and deep-link OAuth.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0",
    "@tauri-apps/plugin-deep-link": "^2.0.0"
  }
}
